/**
 * Creator attribution — a single source of truth so every surface that
 * shows it (splash screen, About panel, and later any report/document
 * Amin exports) says the same thing. Never inline this text elsewhere.
 */
export const CREATOR_NAME_AR = "منى السيد";
export const CREATOR_NAME_EN = "Mona AlSayed";

export const CREATOR_ATTRIBUTION_AR = `من إبداع وتصميم ${CREATOR_NAME_AR}`;
export const CREATOR_ATTRIBUTION_EN = `Created and designed by ${CREATOR_NAME_EN}`;

/**
 * Signature line for anything Amin hands to someone else — a report, a
 * brief, an exported document. Later phases that add real export/report
 * features (Morning Brief, developer workflows, ...) should append this,
 * not invent their own wording.
 */
export const EXPORT_SIGNATURE_AR = `تم إعداد هذا المستند بواسطة أمين — نظام صممته ${CREATOR_NAME_AR}`;
export const EXPORT_SIGNATURE_EN = `Prepared by Amin — a system designed by ${CREATOR_NAME_EN}`;
